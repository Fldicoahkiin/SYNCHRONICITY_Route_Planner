# Claude Notes

Read `AGENTS.md` first. It is the primary project instruction file.

## Claude-Specific Notes

- Use `pnpm` commands from `package.json`
- Keep `.claude/skills/` aligned with the shared project skill set when editing Claude-facing skills
- Do not treat `.claude/settings.local.json` as shared project config
